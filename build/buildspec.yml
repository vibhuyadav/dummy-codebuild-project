version: 0.2

env:
  parameter-store:
    CD_ROLE_ARN: "/cicd/em-iam/${DEPLOYMENT_STAGE_NAME}/role"
    DEFAULT_REGION: "/cicd/em-iam/${DEPLOYMENT_STAGE_NAME}/region"

phases:
  build:
    commands:
    - echo $CD_ROLE_ARN